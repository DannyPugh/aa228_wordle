import numpy as np
import wordle

class WordlePro:
    def __init__(self, prior):
        self.prior = prior

    def make_guess(results):
        for letter in results:
            pass

    def right_place(self, note):
        if len(note) == 3:
            return 1
        elif note.is

try:
    while True
        game = wordle.Wordle(word = wordle.random_answer(), real_words = True)
        results = [0,0,0,0,0]
        for guess in range(6):
            results = game.send_guess(make_guess(results))

